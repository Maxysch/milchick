{
  "name": "milchick",
  "version": "1.0.0",
  "private": true,
  "workspaces": [
    "shared",
    "backend",
    "frontend",
    "agent"
  ],
  "scripts": {
    "dev": "concurrently \"npm run dev -w backend\" \"npm run dev -w frontend\"",
    "dev:backend": "npm run dev -w backend",
    "dev:frontend": "npm run dev -w frontend",
    "dev:agent": "npm run dev -w agent",
    "dev:all": "concurrently \"npm run dev -w backend\" \"npm run dev -w frontend\" \"npm run dev -w agent\"",
    "build": "npm run build -w shared && npm run build -w backend && npm run build -w frontend && npm run build -w agent",
    "typecheck": "npm run typecheck -w shared && npm run typecheck -w backend && npm run typecheck -w frontend && npm run typecheck -w agent"
  },
  "devDependencies": {
    "concurrently": "^9.1.0"
  }
}
